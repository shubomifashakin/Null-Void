repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1 # keep in sync with GITLEAKS_VERSION in secret-scanning.yml
    hooks:
      - id: gitleaks
        args: ["--config", ".gitleaks.toml"]
